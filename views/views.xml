<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="vit_so_warehouse.list">
      <field name="name">vit_so_warehouse list</field>
      <field name="model">vit_so_warehouse.vit_so_warehouse</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="vit_so_warehouse.action_window">
      <field name="name">vit_so_warehouse window</field>
      <field name="res_model">vit_so_warehouse.vit_so_warehouse</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="vit_so_warehouse.action_server">
      <field name="name">vit_so_warehouse server</field>
      <field name="model_id" ref="model_vit_so_warehouse_vit_so_warehouse"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="vit_so_warehouse" id="vit_so_warehouse.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="vit_so_warehouse.menu_1" parent="vit_so_warehouse.menu_root"/>
    <menuitem name="Menu 2" id="vit_so_warehouse.menu_2" parent="vit_so_warehouse.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="vit_so_warehouse.menu_1_list" parent="vit_so_warehouse.menu_1"
              action="vit_so_warehouse.action_window"/>
    <menuitem name="Server to list" id="vit_so_warehouse" parent="vit_so_warehouse.menu_2"
              action="vit_so_warehouse.action_server"/>
    -->
  </data>
</odoo>